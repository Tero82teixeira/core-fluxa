/**
 * TODO(auth): modo de demonstração temporário.
 * Enquanto DEMO_MODE === true, as rotas internas podem ser visualizadas sem
 * sessão autenticada, apenas para revisão das telas. Ao conectar a
 * autenticação real (Supabase Auth), defina como false: o selo some, o acesso
 * direto é bloqueado e /central volta a exigir sessão.
 */
export const DEMO_MODE = true;
